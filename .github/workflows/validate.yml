name: Validate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  docs:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate docs
        shell: pwsh
        run: ./scripts/validate-docs.ps1
